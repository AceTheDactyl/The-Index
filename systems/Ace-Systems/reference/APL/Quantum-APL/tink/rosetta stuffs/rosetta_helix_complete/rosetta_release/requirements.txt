# Rosetta Helix MUD - Build Dependencies

# Required for building executables
pyinstaller>=5.0.0

# Required for WebSocket bridge
websockets>=11.0.0

# Optional: For desktop client (if building GUI version)
# pywebview>=4.0.0
