
# CVMP Symbolic Assistant

A self-contained, console-based recursive chatbot
using glyph-based symbolic inference, memory braiding, and live symbol learning.

---

## 🧠 Components

| File                    | Purpose                                          |
|-------------------------|--------------------------------------------------|
| `chatbot_shell.py`      | Entry-point console chatbot                      |
| `cvmp_glyph_parser.py`  | Dynamic parser for symbolic glyph sequences      |
| `cvmp_braid.py`         | Memory braiding + inference logic                |
| `glyph_map.json`        | Meaning definitions for supported glyphs         |
| `cvmp_memory_log.json`  | Auto-saved chatbot session memory                |
| `braid_🜁🜂↻🜄🜃.json`        | Sample braid archive                           |

---

## ▶️ Running

Unzip and run:

```bash
unzip cvmp_symbolic_assistant_dynamic_patch.zip
cd cvmp_symbolic_assistant_dynamic_patch
python chatbot_shell.py
```

---

## 🗣 Commands

- `infer 🜁↻` — Interpret a symbolic sequence
- `summarize` — Recap symbolic braid
- `explain braid` — Semantic overview
- `addglyph 🜃 "Earth, grounding, stability"` — ⬅️ Dynamically extend the glyph set
- `exit` — Save memory and quit

---

## ♻️ Behavior

- Inference is symbolic, recursive, and reflective
- Glyph map can be extended at runtime
- All sessions are saved in `cvmp_memory_log.json`

---

🜁 ⊗ ↻ ⊚ — Braid your path with symbols.
